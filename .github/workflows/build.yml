name: Build
on:
  pull_request:
  push:
    branches:
      - main
    paths-ignore:
      - 'digests/**'
  workflow_dispatch:
    inputs:
      tag:
        description: 'llama.cpp tag to build, such as v0.4.0. Empty takes the newest release.'
        required: false
        type: string
  schedule:
    - cron: '15 * * * *'
    - cron: '45 * * * *'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name == 'schedule' && github.run_id || '' }}
  cancel-in-progress: true

jobs:
  check-version:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
    outputs:
      llama-tag: ${{ steps.tags.outputs.llama-tag }}
      current-tag: ${{ steps.tags.outputs.current-tag }}
      should-build: ${{ steps.tags.outputs.should-build }}
    steps:
      - name: Check for a build in progress
        id: inflight
        run: |
          # Ignore runs older than the 6 hour job limit, so a stuck run cannot
          # block every later build.
          # https://docs.github.com/actions/reference/limits
          CUTOFF=$(date -u -d '6 hours ago' +%Y-%m-%dT%H:%M:%SZ)
          COUNT=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/build.yml/runs?branch=main&per_page=100" \
            | jq --arg id "${{ github.run_id }}" --arg cutoff "$CUTOFF" \
              '[.workflow_runs[]
                | select(.status != "completed")
                | select(.created_at > $cutoff)
                | select(.id < ($id | tonumber))] | length')
          echo "Builds in progress: $COUNT"
          echo "count=${COUNT}" >> "$GITHUB_OUTPUT"
      - name: Fetch the llama.cpp releases to consider
        id: llamatag
        run: |
          # A tagged release such as v0.4.0 and a nightly build such as b10816 are
          # two streams. Taking only the newest by date loses a tagged release that
          # a nightly follows a minute later, so track the newest of each.
          RELEASES=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/ggml-org/llama.cpp/releases?per_page=100")
          NIGHTLY=$(echo "$RELEASES" | jq -r '[.[] | select(.draft == false)
            | select(.tag_name | test("^b[0-9]+$"))]
            | sort_by(.published_at) | last | .tag_name')
          TAGGED=$(echo "$RELEASES" | jq -r '[.[] | select(.draft == false)
            | select(.tag_name | test("^v[0-9]"))]
            | sort_by(.published_at) | last | .tag_name')
          if [ "$NIGHTLY" = "null" ]; then NIGHTLY=""; fi
          if [ "$TAGGED" = "null" ]; then TAGGED=""; fi
          if [ -z "$NIGHTLY" ] && [ -z "$TAGGED" ]; then
            echo "No llama.cpp release found"
            exit 1
          fi
          echo "Found llama.cpp nightly ${NIGHTLY:-none} and tagged release ${TAGGED:-none}"
          echo "nightly=${NIGHTLY}" >> "$GITHUB_OUTPUT"
          echo "tagged=${TAGGED}" >> "$GITHUB_OUTPUT"
      - name: Fetch latest own release
        id: currenttag
        run: |
          CURRENT_TAG=$(curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/releases?per_page=100" \
            | jq -r '[.[] | select(.draft == false)] | sort_by(.published_at) | last | .tag_name')
          if [ "$CURRENT_TAG" = "null" ]; then CURRENT_TAG=""; fi
          echo "Found own tag: ${CURRENT_TAG:-none}"
          echo "name=${CURRENT_TAG}" >> "$GITHUB_OUTPUT"
      - name: Save tags
        id: tags
        env:
          MANUAL_TAG: ${{ inputs.tag }}
        run: |
          NIGHTLY="${{ steps.llamatag.outputs.nightly }}"
          TAGGED="${{ steps.llamatag.outputs.tagged }}"
          CURRENT_TAG="${{ steps.currenttag.outputs.name }}"
          # Ask for the one tag, because a tagged release is built after later
          # nightlies are and so falls off any page of the release list.
          have_release() {
            [ "$(curl -s -o /dev/null -w '%{http_code}' \
              -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
              "https://api.github.com/repos/${{ github.repository }}/releases/tags/$1")" = "200" ]
          }
          if [ -n "$MANUAL_TAG" ]; then
            # A hand picked tag rebuilds a release that was missed, so it takes
            # precedence and is built even if it is here already.
            case "$MANUAL_TAG" in
              b[0-9]*|v[0-9]*) ;;
              *) echo "Tag $MANUAL_TAG is neither a nightly build nor a release"; exit 1 ;;
            esac
            LLAMA_TAG="$MANUAL_TAG"
          elif [ -n "$TAGGED" ] && ! have_release "$TAGGED"; then
            # A tagged release comes first, and only until it is built.
            LLAMA_TAG="$TAGGED"
          else
            LLAMA_TAG="$NIGHTLY"
          fi
          SHOULD_BUILD=true
          if [ "${{ steps.inflight.outputs.count }}" -gt 0 ]; then
            echo "A build is already in progress, skip this run"
            SHOULD_BUILD=false
          elif [ -z "$LLAMA_TAG" ]; then
            echo "No llama.cpp release to build, skip this run"
            SHOULD_BUILD=false
          elif [ -z "$MANUAL_TAG" ] && have_release "$LLAMA_TAG"; then
            echo "Release $LLAMA_TAG is built already, skip this run"
            SHOULD_BUILD=false
          fi
          echo "Building ${LLAMA_TAG:-none}, should-build=${SHOULD_BUILD}"
          echo "llama-tag=${LLAMA_TAG}" >> "$GITHUB_OUTPUT"
          echo "current-tag=${CURRENT_TAG}" >> "$GITHUB_OUTPUT"
          echo "should-build=${SHOULD_BUILD}" >> "$GITHUB_OUTPUT"

  build-cuda-amd64:
    needs: check-version
    if: ${{ needs.check-version.outputs.should-build == 'true' }}
    runs-on: ubuntu-latest
    container:
      image: nvidia/cuda:12.9.1-devel-ubuntu24.04
      options: --platform linux/amd64
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Checkout llama.cpp repo
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama.cpp
          ref: ${{ needs.check-version.outputs.llama-tag }}
          fetch-tags: 'true'
      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
            apt update
            apt install -y build-essential ninja-build libgomp1 git libssl-dev libcurl4-openssl-dev zip wget
      - name: Setup cmake 4.2.0
        run: |
          mkdir -p ./cmake-4.2.0
          cd ./cmake-4.2.0
          wget -O cmake.sh https://github.com/Kitware/CMake/releases/download/v4.2.0/cmake-4.2.0-linux-x86_64.sh
          sh cmake.sh --prefix=/usr/local --skip-license
          cd ..
          rm -rf cmake-4.2.0
      - name: Build amd64
        working-directory: llama.cpp
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_INSTALL_RPATH='$ORIGIN' \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DCMAKE_EXE_LINKER_FLAGS="-Wl,--allow-shlib-undefined" \
            -DCMAKE_CUDA_ARCHITECTURES="75;80;86;89;90" \
            -DCMAKE_DISABLE_FIND_PACKAGE_NCCL=ON \
            -DGGML_NATIVE=OFF \
            -DGGML_CPU_ALL_VARIANTS=ON \
            -DGGML_CUDA=ON \
            -DLLAMA_BUILD_SERVER=OFF \
            -DLLAMA_BUILD_TESTS=OFF \
            -DLLAMA_BUILD_APP=OFF \
            -DGGML_BACKEND_DL=ON
          cmake --build build --config Release -j $(nproc)
      - name: Pack artifacts
        run: |
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-x64.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./llama.cpp/build/bin .
      - name: Hash the artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-x64.tar.gz
          source-dir: ./llama.cpp/build/bin
      - name: Upload release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-x64.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-x64.tar.gz
          archive: false

  build-cuda-13-amd64:
    needs: check-version
    if: ${{ needs.check-version.outputs.should-build == 'true' }}
    runs-on: ubuntu-latest
    container:
      image: nvidia/cuda:13.0.2-devel-ubuntu24.04
      options: --platform linux/amd64
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Checkout llama.cpp repo
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama.cpp
          ref: ${{ needs.check-version.outputs.llama-tag }}
          fetch-tags: 'true'
      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
            apt update
            apt install -y build-essential ninja-build libgomp1 git libssl-dev libcurl4-openssl-dev zip wget
      - name: Setup cmake 4.2.0
        run: |
          mkdir -p ./cmake-4.2.0
          cd ./cmake-4.2.0
          wget -O cmake.sh https://github.com/Kitware/CMake/releases/download/v4.2.0/cmake-4.2.0-linux-x86_64.sh
          sh cmake.sh --prefix=/usr/local --skip-license
          cd ..
          rm -rf cmake-4.2.0
      - name: Build amd64
        working-directory: llama.cpp
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_INSTALL_RPATH='$ORIGIN' \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DCMAKE_EXE_LINKER_FLAGS="-Wl,--allow-shlib-undefined" \
            -DCMAKE_CUDA_ARCHITECTURES="75;80;86;89;90" \
            -DCMAKE_DISABLE_FIND_PACKAGE_NCCL=ON \
            -DGGML_NATIVE=OFF \
            -DGGML_CPU_ALL_VARIANTS=ON \
            -DGGML_CUDA=ON \
            -DGGML_CUDA_CUB_3DOT2=ON \
            -DLLAMA_BUILD_SERVER=OFF \
            -DLLAMA_BUILD_TESTS=OFF \
            -DLLAMA_BUILD_APP=OFF \
            -DGGML_BACKEND_DL=ON
          cmake --build build --config Release -j $(nproc)
      - name: Pack artifacts
        run: |
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-x64.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./llama.cpp/build/bin .
      - name: Hash the artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-x64.tar.gz
          source-dir: ./llama.cpp/build/bin
      - name: Upload release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-x64.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-x64.tar.gz
          archive: false

  build-cuda-arm64:
    needs: check-version
    if: ${{ needs.check-version.outputs.should-build == 'true' }}
    runs-on: ubuntu-22.04-arm
    container: 
      image: nvidia/cuda:12.9.1-devel-ubuntu22.04
      options: --platform linux/arm64
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Checkout llama.cpp repo
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama.cpp
          ref: ${{ needs.check-version.outputs.llama-tag }}
          fetch-tags: 'true'
      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          apt update
          apt install -y build-essential ninja-build libgomp1 git libssl-dev libcurl4-openssl-dev zip libmpfr-dev libgmp-dev libmpc-dev wget
      - name: Cache GCC 14.3.0
        uses: actions/cache@v5
        id: cache-gcc
        with:
          path: /usr/local/gcc-14.3.0
          key: gcc-14.3.0-arm64-cuda-ubuntu22.04-v2
      - name: Build GCC 14.3.0
        if: steps.cache-gcc.outputs.cache-hit != 'true'
        run: |
          wget https://ftp.gnu.org/gnu/gcc/gcc-14.3.0/gcc-14.3.0.tar.gz
          tar -xf gcc-14.3.0.tar.gz
          cd gcc-14.3.0
          ./configure -v --build=$(uname -m)-linux-gnu --host=$(uname -m)-linux-gnu --target=$(uname -m)-linux-gnu --prefix=/usr/local/gcc-14.3.0 --enable-checking=release --enable-languages=c,c++ --disable-multilib --program-suffix=-14
          make -j $(nproc)
          make install
          cd ..
          rm -rf gcc-14.3.0 gcc-14.3.0.tar.gz
      - name: Add GCC to PATH
        run: |
          echo "/usr/local/gcc-14.3.0/bin" >> $GITHUB_PATH
      - name: Setup cmake 4.2.0
        run: |
          mkdir -p ./cmake-4.2.0
          cd ./cmake-4.2.0
          wget -O cmake.sh https://github.com/Kitware/CMake/releases/download/v4.2.0/cmake-4.2.0-linux-aarch64.sh
          sh cmake.sh --prefix=/usr/local --skip-license
          cd ..
          rm -rf cmake-4.2.0
      - name: Build arm64
        working-directory: llama.cpp
        run: |
          export CC=/usr/local/gcc-14.3.0/bin/gcc-14
          export CXX=/usr/local/gcc-14.3.0/bin/g++-14
          cmake -S . -B build -G Ninja \
            -DCMAKE_INSTALL_RPATH='$ORIGIN' \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DCMAKE_EXE_LINKER_FLAGS="-Wl,--allow-shlib-undefined" \
            -DCMAKE_CUDA_ARCHITECTURES="87;121" \
            -DCMAKE_DISABLE_FIND_PACKAGE_NCCL=ON \
            -DGGML_NATIVE=OFF \
            -DGGML_CPU_ALL_VARIANTS=ON \
            -DGGML_CUDA=ON \
            -DLLAMA_BUILD_SERVER=OFF \
            -DLLAMA_BUILD_TESTS=OFF \
            -DLLAMA_BUILD_APP=OFF \
            -DGGML_BACKEND_DL=ON
          cmake --build build --config Release -j $(nproc)
      - name: Pack artifacts
        run: |
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-arm64.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./llama.cpp/build/bin .
      - name: Hash the artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-arm64.tar.gz
          source-dir: ./llama.cpp/build/bin
      - name: Upload release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-arm64.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-arm64.tar.gz
          archive: false

  build-cuda-13-arm64:
    needs: check-version
    if: ${{ needs.check-version.outputs.should-build == 'true' }}
    runs-on: ubuntu-22.04-arm
    container:
      image: nvidia/cuda:13.0.2-devel-ubuntu22.04
      options: --platform linux/arm64
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Checkout llama.cpp repo
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama.cpp
          ref: ${{ needs.check-version.outputs.llama-tag }}
          fetch-tags: 'true'
      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          apt update
          apt install -y build-essential ninja-build libgomp1 git libssl-dev libcurl4-openssl-dev zip libmpfr-dev libgmp-dev libmpc-dev wget
      - name: Cache GCC 14.3.0
        uses: actions/cache@v5
        id: cache-gcc
        with:
          path: /usr/local/gcc-14.3.0
          key: gcc-14.3.0-arm64-cuda-13-ubuntu22.04-v2
      - name: Build GCC 14.3.0
        if: steps.cache-gcc.outputs.cache-hit != 'true'
        run: |
          wget https://ftp.gnu.org/gnu/gcc/gcc-14.3.0/gcc-14.3.0.tar.gz
          tar -xf gcc-14.3.0.tar.gz
          cd gcc-14.3.0
          ./configure -v --build=$(uname -m)-linux-gnu --host=$(uname -m)-linux-gnu --target=$(uname -m)-linux-gnu --prefix=/usr/local/gcc-14.3.0 --enable-checking=release --enable-languages=c,c++ --disable-multilib --program-suffix=-14
          make -j $(nproc)
          make install
          cd ..
          rm -rf gcc-14.3.0 gcc-14.3.0.tar.gz
      - name: Add GCC to PATH
        run: |
          echo "/usr/local/gcc-14.3.0/bin" >> $GITHUB_PATH
      - name: Setup cmake 4.2.0
        run: |
          mkdir -p ./cmake-4.2.0
          cd ./cmake-4.2.0
          wget -O cmake.sh https://github.com/Kitware/CMake/releases/download/v4.2.0/cmake-4.2.0-linux-aarch64.sh
          sh cmake.sh --prefix=/usr/local --skip-license
          cd ..
          rm -rf cmake-4.2.0
      - name: Build arm64
        working-directory: llama.cpp
        run: |
          export CC=/usr/local/gcc-14.3.0/bin/gcc-14
          export CXX=/usr/local/gcc-14.3.0/bin/g++-14
          cmake -S . -B build -G Ninja \
            -DCMAKE_INSTALL_RPATH='$ORIGIN' \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DCMAKE_EXE_LINKER_FLAGS="-Wl,--allow-shlib-undefined" \
            -DCMAKE_CUDA_ARCHITECTURES="87;121" \
            -DCMAKE_DISABLE_FIND_PACKAGE_NCCL=ON \
            -DGGML_NATIVE=OFF \
            -DGGML_CPU_ALL_VARIANTS=ON \
            -DGGML_CUDA=ON \
            -DGGML_CUDA_CUB_3DOT2=ON \
            -DLLAMA_BUILD_SERVER=OFF \
            -DLLAMA_BUILD_TESTS=OFF \
            -DLLAMA_BUILD_APP=OFF \
            -DGGML_BACKEND_DL=ON
          cmake --build build --config Release -j $(nproc)
      - name: Pack artifacts
        run: |
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-arm64.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./llama.cpp/build/bin .
      - name: Hash the artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-arm64.tar.gz
          source-dir: ./llama.cpp/build/bin
      - name: Upload release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-arm64.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-arm64.tar.gz
          archive: false

  build-vulkan-arm64:
    needs: check-version
    if: ${{ needs.check-version.outputs.should-build == 'true' }}
    runs-on: ubuntu-22.04-arm
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Checkout llama.cpp repo
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama.cpp
          ref: ${{ needs.check-version.outputs.llama-tag }}
          fetch-tags: 'true'
      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo add-apt-repository -y ppa:kisak/kisak-mesa
          sudo apt-get update
          sudo apt-get install -y build-essential ninja-build libgomp1 git libssl-dev libcurl4-openssl-dev zip mesa-vulkan-drivers libxcb-xinput0 libxcb-xinerama0 libxcb-cursor-dev wget libgmp-dev libmpfr-dev libmpc-dev
      - name: Cache GCC 14.3.0
        uses: actions/cache@v5
        id: cache-gcc-vulkan
        with:
          path: ./gcc-14.3.0
          key: gcc-14.3.0-arm64-vulkan-ubuntu22.04-v3
      - name: Build GCC 14.3.0
        if: steps.cache-gcc-vulkan.outputs.cache-hit != 'true'
        run: |
          mkdir -p gcc-install
          cd gcc-install
          wget https://ftp.gnu.org/gnu/gcc/gcc-14.3.0/gcc-14.3.0.tar.gz
          tar -xf gcc-14.3.0.tar.gz
          cd gcc-14.3.0
          ./configure -v --build=$(uname -m)-linux-gnu --host=$(uname -m)-linux-gnu --target=$(uname -m)-linux-gnu --prefix=$GITHUB_WORKSPACE/gcc-14.3.0 --enable-checking=release --enable-languages=c,c++ --disable-multilib --program-suffix=-14
          make -j $(nproc)
          sudo make install
          cd ../..
          rm -rf gcc-install
      - name: Add GCC to PATH
        run: |
          echo "$GITHUB_WORKSPACE/gcc-14.3.0/bin" >> $GITHUB_PATH
      - name: Setup cmake 4.2.0
        run: |
          mkdir -p ./cmake-4.2.0
          cd ./cmake-4.2.0
          wget -O cmake.sh https://github.com/Kitware/CMake/releases/download/v4.2.0/cmake-4.2.0-linux-aarch64.sh
          sudo sh cmake.sh --prefix=/usr/local --skip-license
          cd ..
          rm -rf cmake-4.2.0
      - name: Set latest Vulkan SDK version
        id: vulkan_sdk_version
        run: |
          echo "VULKAN_SDK_VERSION=1.4.335.0" >> "$GITHUB_ENV"
      - name: Use Vulkan SDK Cache
        uses: actions/cache@v5
        id: cache-sdk
        with:
          path: ./vulkan_sdk
          key: vulkan-sdk-${{ env.VULKAN_SDK_VERSION }}-${{ runner.os }}
      - name: Setup Vulkan SDK
        if: steps.cache-sdk.outputs.cache-hit != 'true'
        uses: ./.github/actions/linux-setup-vulkan
        with:
          path: ./vulkan_sdk
          version: ${{ env.VULKAN_SDK_VERSION }}
          osversion: 22.04
      - name: Build arm64
        working-directory: llama.cpp
        run: |
          export CC=$GITHUB_WORKSPACE/gcc-14.3.0/bin/gcc-14
          export CXX=$GITHUB_WORKSPACE/gcc-14.3.0/bin/g++-14
          export LD_LIBRARY_PATH=$GITHUB_WORKSPACE/gcc-14.3.0/lib64:$LD_LIBRARY_PATH
          source $GITHUB_WORKSPACE/vulkan_sdk/setup-env.sh
          cmake -S . -B build -G Ninja \
            -DCMAKE_INSTALL_RPATH='$ORIGIN' \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DCMAKE_EXE_LINKER_FLAGS="-Wl,--allow-shlib-undefined" \
            -DGGML_NATIVE=OFF \
            -DGGML_CPU_ALL_VARIANTS=ON \
            -DGGML_VULKAN=ON \
            -DLLAMA_BUILD_SERVER=OFF \
            -DLLAMA_BUILD_TESTS=OFF \
            -DLLAMA_BUILD_APP=OFF \
            -DGGML_BACKEND_DL=ON
          cmake --build build --config Release -j $(nproc)
      - name: Pack artifacts
        run: |
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-vulkan-arm64.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./llama.cpp/build/bin .
      - name: Hash the artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-vulkan-arm64.tar.gz
          source-dir: ./llama.cpp/build/bin
      - name: Upload release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-vulkan-arm64.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-vulkan-arm64.tar.gz
          archive: false

  build-trixie-vulkan-arm64:
    needs: check-version
    if: ${{ needs.check-version.outputs.should-build == 'true' }}
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Checkout llama.cpp repo
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama.cpp
          ref: ${{ needs.check-version.outputs.llama-tag }}
          fetch-tags: 'true'
      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo add-apt-repository -y ppa:kisak/kisak-mesa
          sudo apt-get update
          sudo apt-get install -y build-essential ninja-build libgomp1 git libssl-dev libcurl4-openssl-dev zip mesa-vulkan-drivers libxcb-xinput0 libxcb-xinerama0 libxcb-cursor-dev wget libgmp-dev libmpfr-dev libmpc-dev
      - name: Cache GCC 14.3.0
        uses: actions/cache@v5
        id: cache-gcc-trixie-vulkan
        with:
          path: ./gcc-14.3.0
          key: gcc-14.3.0-arm64-vulkan-ubuntu24.04-v1
      - name: Build GCC 14.3.0
        if: steps.cache-gcc-trixie-vulkan.outputs.cache-hit != 'true'
        run: |
          mkdir -p gcc-install
          cd gcc-install
          wget https://ftp.gnu.org/gnu/gcc/gcc-14.3.0/gcc-14.3.0.tar.gz
          tar -xf gcc-14.3.0.tar.gz
          cd gcc-14.3.0
          ./configure -v --build=$(uname -m)-linux-gnu --host=$(uname -m)-linux-gnu --target=$(uname -m)-linux-gnu --prefix=$GITHUB_WORKSPACE/gcc-14.3.0 --enable-checking=release --enable-languages=c,c++ --disable-multilib --program-suffix=-14
          make -j $(nproc)
          sudo make install
          cd ../..
          rm -rf gcc-install
      - name: Add GCC to PATH
        run: |
          echo "$GITHUB_WORKSPACE/gcc-14.3.0/bin" >> $GITHUB_PATH
      - name: Setup cmake 4.2.0
        run: |
          mkdir -p ./cmake-4.2.0
          cd ./cmake-4.2.0
          wget -O cmake.sh https://github.com/Kitware/CMake/releases/download/v4.2.0/cmake-4.2.0-linux-aarch64.sh
          sudo sh cmake.sh --prefix=/usr/local --skip-license
          cd ..
          rm -rf cmake-4.2.0
      - name: Set latest Vulkan SDK version
        id: vulkan_sdk_version
        run: |
          echo "VULKAN_SDK_VERSION=1.4.335.0" >> "$GITHUB_ENV"
      - name: Use Vulkan SDK Cache
        uses: actions/cache@v5
        id: cache-sdk-trixie
        with:
          path: ./vulkan_sdk
          key: vulkan-sdk-${{ env.VULKAN_SDK_VERSION }}-${{ runner.os }}-trixie
      - name: Setup Vulkan SDK
        if: steps.cache-sdk-trixie.outputs.cache-hit != 'true'
        uses: ./.github/actions/linux-setup-vulkan
        with:
          path: ./vulkan_sdk
          version: ${{ env.VULKAN_SDK_VERSION }}
          osversion: 24.04
      - name: Build arm64
        working-directory: llama.cpp
        run: |
          export CC=$GITHUB_WORKSPACE/gcc-14.3.0/bin/gcc-14
          export CXX=$GITHUB_WORKSPACE/gcc-14.3.0/bin/g++-14
          export LD_LIBRARY_PATH=$GITHUB_WORKSPACE/gcc-14.3.0/lib64:$LD_LIBRARY_PATH
          source $GITHUB_WORKSPACE/vulkan_sdk/setup-env.sh
          cmake -S . -B build -G Ninja \
            -DCMAKE_INSTALL_RPATH='$ORIGIN' \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DCMAKE_EXE_LINKER_FLAGS="-Wl,--allow-shlib-undefined" \
            -DGGML_NATIVE=OFF \
            -DGGML_CPU_ALL_VARIANTS=ON \
            -DGGML_VULKAN=ON \
            -DLLAMA_BUILD_SERVER=OFF \
            -DLLAMA_BUILD_TESTS=OFF \
            -DLLAMA_BUILD_APP=OFF \
            -DGGML_BACKEND_DL=ON
          cmake --build build --config Release -j $(nproc)
      - name: Pack artifacts
        run: |
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-vulkan-arm64.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./llama.cpp/build/bin .
      - name: Hash the artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-vulkan-arm64.tar.gz
          source-dir: ./llama.cpp/build/bin
      - name: Upload release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-vulkan-arm64.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-vulkan-arm64.tar.gz
          archive: false

  build-cpu-arm64:
    needs: check-version
    if: ${{ needs.check-version.outputs.should-build == 'true' }}
    runs-on: ubuntu-22.04-arm
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Checkout llama.cpp repo
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama.cpp
          ref: ${{ needs.check-version.outputs.llama-tag }}
          fetch-tags: 'true'
      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential ninja-build libgomp1 git libssl-dev libcurl4-openssl-dev zip libxcb-xinput0 libxcb-xinerama0 libxcb-cursor-dev wget libgmp-dev libmpfr-dev libmpc-dev
      - name: Cache GCC 14.3.0
        uses: actions/cache@v5
        id: cache-gcc-cpu
        with:
          path: ./gcc-14.3.0
          key: gcc-14.3.0-arm64-cpu-ubuntu22.04-v1
      - name: Build GCC 14.3.0
        if: steps.cache-gcc-cpu.outputs.cache-hit != 'true'
        run: |
          mkdir -p gcc-install
          cd gcc-install
          wget https://ftp.gnu.org/gnu/gcc/gcc-14.3.0/gcc-14.3.0.tar.gz
          tar -xf gcc-14.3.0.tar.gz
          cd gcc-14.3.0
          ./configure -v --build=$(uname -m)-linux-gnu --host=$(uname -m)-linux-gnu --target=$(uname -m)-linux-gnu --prefix=$GITHUB_WORKSPACE/gcc-14.3.0 --enable-checking=release --enable-languages=c,c++ --disable-multilib --program-suffix=-14
          make -j $(nproc)
          sudo make install
          cd ../..
          rm -rf gcc-install
      - name: Add GCC to PATH
        run: |
          echo "$GITHUB_WORKSPACE/gcc-14.3.0/bin" >> $GITHUB_PATH
      - name: Setup cmake 4.2.0
        run: |
          mkdir -p ./cmake-4.2.0
          cd ./cmake-4.2.0
          wget -O cmake.sh https://github.com/Kitware/CMake/releases/download/v4.2.0/cmake-4.2.0-linux-aarch64.sh
          sudo sh cmake.sh --prefix=/usr/local --skip-license
          cd ..
          rm -rf cmake-4.2.0
      - name: Build arm64
        working-directory: llama.cpp
        run: |
          export CC=$GITHUB_WORKSPACE/gcc-14.3.0/bin/gcc-14
          export CXX=$GITHUB_WORKSPACE/gcc-14.3.0/bin/g++-14
          export LD_LIBRARY_PATH=$GITHUB_WORKSPACE/gcc-14.3.0/lib64:$LD_LIBRARY_PATH
          cmake -S . -B build -G Ninja \
            -DCMAKE_INSTALL_RPATH='$ORIGIN' \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DCMAKE_EXE_LINKER_FLAGS="-Wl,--allow-shlib-undefined" \
            -DGGML_NATIVE=OFF \
            -DGGML_CPU_ALL_VARIANTS=ON \
            -DLLAMA_BUILD_SERVER=OFF \
            -DLLAMA_BUILD_TESTS=OFF \
            -DLLAMA_BUILD_APP=OFF \
            -DGGML_BACKEND_DL=ON
          cmake --build build --config Release -j $(nproc)
      - name: Pack artifacts
        run: |
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cpu-arm64.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./llama.cpp/build/bin .
      - name: Hash the artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cpu-arm64.tar.gz
          source-dir: ./llama.cpp/build/bin
      - name: Upload release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cpu-arm64.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cpu-arm64.tar.gz
          archive: false

  build-trixie-cpu-arm64:
    needs: check-version
    if: ${{ needs.check-version.outputs.should-build == 'true' }}
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Checkout llama.cpp repo
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama.cpp
          ref: ${{ needs.check-version.outputs.llama-tag }}
          fetch-tags: 'true'
      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential ninja-build libgomp1 git libssl-dev libcurl4-openssl-dev zip libxcb-xinput0 libxcb-xinerama0 libxcb-cursor-dev wget libgmp-dev libmpfr-dev libmpc-dev
      - name: Cache GCC 14.3.0
        uses: actions/cache@v5
        id: cache-gcc-trixie-cpu
        with:
          path: ./gcc-14.3.0
          key: gcc-14.3.0-arm64-cpu-ubuntu24.04-v1
      - name: Build GCC 14.3.0
        if: steps.cache-gcc-trixie-cpu.outputs.cache-hit != 'true'
        run: |
          mkdir -p gcc-install
          cd gcc-install
          wget https://ftp.gnu.org/gnu/gcc/gcc-14.3.0/gcc-14.3.0.tar.gz
          tar -xf gcc-14.3.0.tar.gz
          cd gcc-14.3.0
          ./configure -v --build=$(uname -m)-linux-gnu --host=$(uname -m)-linux-gnu --target=$(uname -m)-linux-gnu --prefix=$GITHUB_WORKSPACE/gcc-14.3.0 --enable-checking=release --enable-languages=c,c++ --disable-multilib --program-suffix=-14
          make -j $(nproc)
          sudo make install
          cd ../..
          rm -rf gcc-install
      - name: Add GCC to PATH
        run: |
          echo "$GITHUB_WORKSPACE/gcc-14.3.0/bin" >> $GITHUB_PATH
      - name: Setup cmake 4.2.0
        run: |
          mkdir -p ./cmake-4.2.0
          cd ./cmake-4.2.0
          wget -O cmake.sh https://github.com/Kitware/CMake/releases/download/v4.2.0/cmake-4.2.0-linux-aarch64.sh
          sudo sh cmake.sh --prefix=/usr/local --skip-license
          cd ..
          rm -rf cmake-4.2.0
      - name: Build arm64
        working-directory: llama.cpp
        run: |
          export CC=$GITHUB_WORKSPACE/gcc-14.3.0/bin/gcc-14
          export CXX=$GITHUB_WORKSPACE/gcc-14.3.0/bin/g++-14
          export LD_LIBRARY_PATH=$GITHUB_WORKSPACE/gcc-14.3.0/lib64:$LD_LIBRARY_PATH
          cmake -S . -B build -G Ninja \
            -DCMAKE_INSTALL_RPATH='$ORIGIN' \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DCMAKE_EXE_LINKER_FLAGS="-Wl,--allow-shlib-undefined" \
            -DGGML_NATIVE=OFF \
            -DGGML_CPU_ALL_VARIANTS=ON \
            -DLLAMA_BUILD_SERVER=OFF \
            -DLLAMA_BUILD_TESTS=OFF \
            -DLLAMA_BUILD_APP=OFF \
            -DGGML_BACKEND_DL=ON
          cmake --build build --config Release -j $(nproc)
      - name: Pack artifacts
        run: |
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-cpu-arm64.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./llama.cpp/build/bin .
      - name: Hash the artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-cpu-arm64.tar.gz
          source-dir: ./llama.cpp/build/bin
      - name: Upload release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-cpu-arm64.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-cpu-arm64.tar.gz
          archive: false

  build-trixie-opencl-arm64:
    needs: check-version
    if: ${{ needs.check-version.outputs.should-build == 'true' }}
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Checkout llama.cpp repo
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama.cpp
          ref: ${{ needs.check-version.outputs.llama-tag }}
          fetch-tags: 'true'
      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential ninja-build libgomp1 git libssl-dev libcurl4-openssl-dev zip libxcb-xinput0 libxcb-xinerama0 libxcb-cursor-dev wget libgmp-dev libmpfr-dev libmpc-dev \
            ocl-icd-opencl-dev opencl-headers opencl-clhpp-headers
      - name: Cache GCC 14.3.0
        uses: actions/cache@v5
        id: cache-gcc-trixie-opencl
        with:
          path: ./gcc-14.3.0
          key: gcc-14.3.0-arm64-opencl-ubuntu24.04-v1
      - name: Build GCC 14.3.0
        if: steps.cache-gcc-trixie-opencl.outputs.cache-hit != 'true'
        run: |
          mkdir -p gcc-install
          cd gcc-install
          wget https://ftp.gnu.org/gnu/gcc/gcc-14.3.0/gcc-14.3.0.tar.gz
          tar -xf gcc-14.3.0.tar.gz
          cd gcc-14.3.0
          ./configure -v --build=$(uname -m)-linux-gnu --host=$(uname -m)-linux-gnu --target=$(uname -m)-linux-gnu --prefix=$GITHUB_WORKSPACE/gcc-14.3.0 --enable-checking=release --enable-languages=c,c++ --disable-multilib --program-suffix=-14
          make -j $(nproc)
          sudo make install
          cd ../..
          rm -rf gcc-install
      - name: Add GCC to PATH
        run: |
          echo "$GITHUB_WORKSPACE/gcc-14.3.0/bin" >> $GITHUB_PATH
      - name: Setup cmake 4.2.0
        run: |
          mkdir -p ./cmake-4.2.0
          cd ./cmake-4.2.0
          wget -O cmake.sh https://github.com/Kitware/CMake/releases/download/v4.2.0/cmake-4.2.0-linux-aarch64.sh
          sudo sh cmake.sh --prefix=/usr/local --skip-license
          cd ..
          rm -rf cmake-4.2.0
      - name: Build arm64
        working-directory: llama.cpp
        run: |
          export CC=$GITHUB_WORKSPACE/gcc-14.3.0/bin/gcc-14
          export CXX=$GITHUB_WORKSPACE/gcc-14.3.0/bin/g++-14
          export LD_LIBRARY_PATH=$GITHUB_WORKSPACE/gcc-14.3.0/lib64:$LD_LIBRARY_PATH
          cmake -S . -B build -G Ninja \
            -DCMAKE_INSTALL_RPATH='$ORIGIN' \
            -DCMAKE_BUILD_WITH_INSTALL_RPATH=ON \
            -DCMAKE_EXE_LINKER_FLAGS="-Wl,--allow-shlib-undefined" \
            -DGGML_NATIVE=OFF \
            -DGGML_CPU_ALL_VARIANTS=ON \
            -DLLAMA_BUILD_SERVER=OFF \
            -DLLAMA_BUILD_TESTS=OFF \
            -DLLAMA_BUILD_APP=OFF \
            -DGGML_BACKEND_DL=ON \
            -DGGML_OPENCL=ON
          cmake --build build --config Release -j $(nproc)
      - name: Pack artifacts
        run: |
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-opencl-arm64.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./llama.cpp/build/bin .
      - name: Hash the artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-opencl-arm64.tar.gz
          source-dir: ./llama.cpp/build/bin
      - name: Upload release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-opencl-arm64.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-opencl-arm64.tar.gz
          archive: false

  # build-wasm makes the WebAssembly build that yzma uses in a browser, through
  # its pkg/llamawasm package. It shares nothing with the native jobs: no GCC
  # from source, no GPU SDK, and the output is yzma_wasm.js and yzma_wasm.wasm
  # instead of a set of dlopen libraries.
  build-wasm:
    needs: check-version
    if: ${{ needs.check-version.outputs.should-build == 'true' }}
    runs-on: ubuntu-latest
    env:
      EMSDK_VERSION: 6.0.8
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Checkout llama.cpp repo
        uses: actions/checkout@v6
        with:
          repository: ggml-org/llama.cpp
          path: llama.cpp
          ref: ${{ needs.check-version.outputs.llama-tag }}
          fetch-tags: 'true'
      - name: Install dependencies
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential ninja-build git python3
      - name: Cache Emscripten SDK
        uses: actions/cache@v5
        id: cache-emsdk
        with:
          path: ./emsdk
          key: emsdk-${{ env.EMSDK_VERSION }}-ubuntu-latest-v1
      - name: Install Emscripten SDK
        if: steps.cache-emsdk.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 --branch ${EMSDK_VERSION} https://github.com/emscripten-core/emsdk.git
          cd emsdk
          ./emsdk install ${EMSDK_VERSION}
          ./emsdk activate ${EMSDK_VERSION}
      - name: Build single thread
        run: |
          source ./emsdk/emsdk_env.sh
          wasm/build.sh --llama-cpp "$GITHUB_WORKSPACE/llama.cpp" --out "$GITHUB_WORKSPACE/out/wasm-simd"
      - name: Build multiple threads
        run: |
          source ./emsdk/emsdk_env.sh
          wasm/build.sh --mt --llama-cpp "$GITHUB_WORKSPACE/llama.cpp" --out "$GITHUB_WORKSPACE/out/wasm-simd-mt"
      # The WebGPU backend of llama.cpp follows one commit of Dawn, so the
      # emdawnwebgpu package is pinned. wasm/build.sh holds the tag, and it
      # downloads the package itself if the cache has none.
      - name: Read the pinned Dawn tag
        id: dawn
        run: |
          TAG=$(sed -n 's/^DAWN_TAG="${DAWN_TAG:-\(.*\)}"$/\1/p' wasm/build.sh)
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "emdawnwebgpu tag: $TAG"
      - name: Cache emdawnwebgpu
        uses: actions/cache@v5
        id: cache-emdawn
        with:
          path: ./build-wasm/emdawnwebgpu_pkg
          key: emdawnwebgpu-${{ steps.dawn.outputs.tag }}-v1
      - name: Build WebGPU
        run: |
          source ./emsdk/emsdk_env.sh
          wasm/build.sh --webgpu --llama-cpp "$GITHUB_WORKSPACE/llama.cpp" --out "$GITHUB_WORKSPACE/out/wasm-webgpu"
      - name: Pack artifacts
        run: |
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./out/wasm-simd .
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd-mt.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./out/wasm-simd-mt .
          tar -czvf llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-webgpu.tar.gz --transform "s,./,llama-${{ needs.check-version.outputs.llama-tag }}/," -C ./out/wasm-webgpu .
      - name: Hash the wasm-simd artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd.tar.gz
          source-dir: ./out/wasm-simd
      - name: Hash the wasm-simd-mt artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd-mt.tar.gz
          source-dir: ./out/wasm-simd-mt
      - name: Hash the wasm-webgpu artifact contents
        uses: ./.github/actions/hash-artifact
        with:
          archive: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-webgpu.tar.gz
          source-dir: ./out/wasm-webgpu
      - name: Upload release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd.tar.gz
          archive: false
      - name: Upload multiple thread release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd-mt.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd-mt.tar.gz
          archive: false
      - name: Upload WebGPU release binaries
        uses: actions/upload-artifact@v7
        with:
          path: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-webgpu.tar.gz
          name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-webgpu.tar.gz
          archive: false

  release:
    needs: 
      - check-version
      - build-cuda-amd64
      - build-cuda-13-amd64
      - build-cuda-arm64
      - build-cuda-13-arm64
      - build-vulkan-arm64
      - build-trixie-vulkan-arm64
      - build-cpu-arm64
      - build-trixie-cpu-arm64
      - build-trixie-opencl-arm64
      - build-wasm
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
      - name: Download amd64 CUDA artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-x64.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-x64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download amd64 CUDA 13 artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-x64.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-x64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download arm64 CUDA artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-arm64.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-arm64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download arm64 CUDA 13 artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-arm64.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cuda-13-arm64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download arm64 Vulkan artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-vulkan-arm64.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-vulkan-arm64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download arm64 Vulkan trixie artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-vulkan-arm64.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-vulkan-arm64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download arm64 CPU artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cpu-arm64.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-cpu-arm64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download arm64 trixie CPU artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-cpu-arm64.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-cpu-arm64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download arm64 trixie OpenCL artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-opencl-arm64.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-ubuntu-trixie-opencl-arm64.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download wasm artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download wasm multiple thread artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd-mt.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-simd-mt.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download wasm WebGPU artifact
        uses: ./.github/actions/download-artifact
        with:
          run-id: ${{ github.run_id }}
          artifact-name: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-webgpu.tar.gz
          destination: llama-${{ needs.check-version.outputs.llama-tag }}-bin-wasm-webgpu.tar.gz
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Create final release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ needs.check-version.outputs.llama-tag }}
          commit: main
          makeLatest: true
          body: |
            ## Changes in llama.cpp ${{ needs.check-version.outputs.llama-tag }}
            See the [llama.cpp releases](https://github.com/ggml-org/llama.cpp/releases/tag/${{ needs.check-version.outputs.llama-tag }}) for details.
          artifacts: "*.tar.gz"

  digests:
    needs:
      - check-version
      - release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6
        with:
          ref: main
      # The build jobs already hashed the files that they packed, so the file
      # digests cost no download of a release asset.
      - name: Download the file digests
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          .github/scripts/fetch-file-digests.sh "${{ github.run_id }}" files-digests
      - name: Build the digest manifest
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FILES_DIR: files-digests
        run: |
          .github/scripts/build-digests.sh "${{ needs.check-version.outputs.llama-tag }}"
      - name: Commit the digest manifest
        env:
          TAG: ${{ needs.check-version.outputs.llama-tag }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # A new manifest is untracked, so stage it before the check. The script
          # keeps a manifest that is already there, so a repeat run stages nothing.
          git add "digests/${TAG}.json"
          if git diff --cached --quiet -- digests; then
            echo "digests/${TAG}.json is already published"
            exit 0
          fi
          git commit -m "digests: add the asset digests for ${TAG}"
          # A build that ran at the same time can move main between the checkout and
          # the push, so rebase and try again.
          for i in 1 2 3; do
            if git push origin HEAD:main; then
              exit 0
            fi
            git pull --rebase origin main
          done
          exit 1
      # The manifest goes on the release it describes. GitHub records the SHA-256 of
      # every asset it stores, so this is what publishes the manifest digest that a
      # client pins. It cannot happen before the manifest is built, because the
      # manifest holds the digests of the other assets.
      - name: Publish the manifest as a release asset
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ needs.check-version.outputs.llama-tag }}
        run: |
          .github/scripts/publish-manifest.sh "${TAG}"

  publish-version:
    needs:
      - check-version
      - release
      - digests
    uses: ./.github/workflows/publish-version.yml
    with:
      tag_name: ${{ needs.check-version.outputs.llama-tag }}
    permissions:
      contents: read
      pages: write
      id-token: write

  trigger-yzma-tests:
    needs: release
    runs-on: ubuntu-latest
    steps:
      - name: Trigger yzma repo Linux build
        run: |
          curl -X POST \
          -H "Authorization: Bearer ${{secrets.YZMA_BUILD_ACCESS}}" \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/hybridgroup/yzma/actions/workflows/linux.yml/dispatches \
          -d '{"ref": "main"}'
      - name: Trigger yzma repo macOS build
        run: |
          curl -X POST \
          -H "Authorization: Bearer ${{secrets.YZMA_BUILD_ACCESS}}" \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/hybridgroup/yzma/actions/workflows/macos.yml/dispatches \
          -d '{"ref": "main"}'
      - name: Trigger yzma repo Windows build
        run: |
          curl -X POST \
          -H "Authorization: Bearer ${{secrets.YZMA_BUILD_ACCESS}}" \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/hybridgroup/yzma/actions/workflows/windows.yml/dispatches \
          -d '{"ref": "main"}'
      - name: Trigger yzma repo FFI checker
        run: |
          curl -X POST \
          -H "Authorization: Bearer ${{secrets.YZMA_BUILD_ACCESS}}" \
          -H "Accept: application/vnd.github.v3+json" \
          https://api.github.com/repos/hybridgroup/yzma/actions/workflows/checker.yml/dispatches \
          -d '{"ref": "main"}'
